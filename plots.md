# Predicate Plots

The former predicate plot gallery compared removed compatibility paths. The
gallery is intentionally retired until an exact-hyperreal visualizer is rebuilt.

Future plot tooling should sample through `hyperreal::Real` or
`hyperreal::Real` and color only exact, filtered, refined, and undecided
provenance.
